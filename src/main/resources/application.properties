
spring.application.name=fc

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/fc
spring.datasource.username=postgres
spring.datasource.password=Adrin@123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=adrinbershik.2201008@srec.ac.in
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration (read from .env)
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

spring.config.import=optional:file:.env[.properties]
